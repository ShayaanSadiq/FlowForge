# Copy to .env.local for Vite dev server, or set in Vercel project settings.

# Production: your AWS API URL (ALB / custom domain), e.g. https://api.yourdomain.com
# Local dev: leave empty — Vite proxies to http://localhost:8080
VITE_API_BASE_URL=
